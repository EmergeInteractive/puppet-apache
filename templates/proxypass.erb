# file managed by puppet

<Proxy <%= url %>*>
    Order deny,allow
    Allow from all
</Proxy>

ProxyPassMatch <%= location %> <%= url -%> <%= params.join(' ') %>

<% unless app_name == "" -%>
	ProxyPassReverseCookiePath /<%= app_name -%> /
<% end -%>
